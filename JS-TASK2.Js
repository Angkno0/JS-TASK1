// Lag en funksjon som tar inn en tekst og en boolean. Basert på verdien i boolean'en skal den enten returnere teksten med store eller små bokstaver.

function TextChange(text){
console.log(text.lenght);
console.log(text);

}